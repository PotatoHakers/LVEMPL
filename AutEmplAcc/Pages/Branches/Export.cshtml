@page
@model AutEmplAcc.Pages.Branches.ExportModel
@{
}

<h1>Экспорт отчета в Word</h1>

<dl class="row">
    <dt class="col-sm-2">Название филиала</dt>
    <dd class="col-sm-10">@Model.Branch.Name</dd>
    <dt class="col-sm-2">Адрес</dt>
    <dd class="col-sm-10">@Model.Branch.Address</dd>
</dl>

<form method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit">Экспортировать в Word</button>
</form>